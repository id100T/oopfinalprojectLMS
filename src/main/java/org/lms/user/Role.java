package org.lms.user;

/**
 * Role enum defines the possible roles a user can have in the system.
 * It is used to assign roles to users, such as ADMIN or VISITOR.
 */
public enum Role {
    ADMIN,    // Role for administrative users
    VISITOR   // Role for regular visitors
}
